package com.example.demo.services;

import com.example.demo.entities.dto.OdontologoDto;
import com.example.demo.entities.dto.PacienteDto;
import com.example.demo.entities.dto.TurnoDto;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;


@SpringBootTest
class TurnoServiceTest {
  /*  private static TurnoService turnoService;
    @Autowired
    public  TurnoServiceTest (TurnoService turnoService) {
        this.turnoService = turnoService;
    }
    @Test
    public void guardar() {
        TurnoDto t =new TurnoDto();
        t.setFecha("01/17/2003");
        t.setOdontologo(String.valueOf(new OdontologoDto()));
        t.setPaciente(String.valueOf(new PacienteDto()));
        TurnoDto tGuardado = turnoService.guardar(t);
        Assertions.assertTrue(tGuardado.getId() != null);
    }
*/


}
